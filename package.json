{
  "private": true,
  "dependencies": {
    "music-metadata-browser": "1.0.0",
    "streaming-http-token-reader": "0.0.3"
  },
  "devDependencies": {
    "parcel-bundler": "^1.11.0"
  },
  "scripts": {
    "build": "parcel build index.js --out-file musicmetadata.min.js --no-source-maps"
  },
  "alias": {
    "stream": "readable-stream"
  }
}
